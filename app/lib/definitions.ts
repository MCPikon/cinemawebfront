export type Movie = {
    imdbId: string, 
    title: string, 
    duration: string, 
    releaseDate: string, 
    poster: string
}

export type Series = {
    imdbId: string, 
    title: string, 
    numberOfSeasons: number, 
    releaseDate: string, 
    poster: string
}